
enum {{reference-type}} implements fi.tnie.db.ent.Reference {
    {{reference-constants}}
    ;
    
    private String identifier;
    private ReferenceType<?, ?> type;
        
    {{reference-type}}(ReferenceType<?, ?> type) {    
    	this.type = type;
    }
    
    {{reference-type}}(String identifier, ReferenceType<?, ?> type) {
    	this(type);
        this.identifier = identifier;
    }    
    
    @Override
    public String identifier() {
        return (identifier == null) ? name() : identifier;
    }

    public ReferenceType<?, ?> type() {
    	return this.type;
    }    
}    
    