
enum {{attribute-type}} implements fi.tnie.db.ent.Attribute {
    {{attribute-constants}}
    ;
    
    private String identifier;
    private PrimitiveType<?> type;
        
    {{attribute-type}}(PrimitiveType<?> type) {
    	this.type = type;
    }
    
    {{attribute-type}}(String identifier, PrimitiveType<?> type) {
    	this(type);
        this.identifier = identifier;
    }    
    
    @Override
    public String identifier() {
        return (identifier == null) ? name() : identifier;
    }

    public PrimitiveType<?> type() {
    	return this.type;
    }    
}    
    